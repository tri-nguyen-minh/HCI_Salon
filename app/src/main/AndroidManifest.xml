<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.hci.manager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HCI_Salon_Manager">
        <activity android:name=".activities.BookingViewActivity"></activity>
        <activity android:name=".activities.DiscountActivity" />
        <activity android:name=".activities.ServiceActivity" />
        <activity android:name=".activities.ScheduleActivity" />
        <activity android:name=".activities.ServiceTypeActivity" />
        <activity android:name=".activities.NotificationActivity" />
        <activity android:name=".activities.HomeActivity" />
        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>